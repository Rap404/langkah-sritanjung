export interface Transportation {
    id: number
    name: string
    slug: string
    description: string
    address: string
    telephone: string
    price: string
    url: string
    image: string
    created_at: string
}